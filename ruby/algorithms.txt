ROBOT ELEVATOR
- Move forward until the top row is reached (Row 1).
- Move left along Row 1 until the 1st column is reached (column A).
- Until the floor_number is the same as the button_number 
	-until you reach the last column (column D)
		-move right one button
	-move down one row and back to column A
-Press button


